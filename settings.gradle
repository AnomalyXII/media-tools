rootProject.name = 'media-tools'

// APIs
include 'media-tools-api'
findProject(':media-tools-api')?.name = 'api'

// Implementations
include 'media-tools-impl-plex'
findProject(':media-tools-impl-plex')?.name = 'impl-plex'